services:
  jit-tool:
    build: .
    container_name: jumpserver-jit-tool
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - jit_data:/app/data
    environment:
      - JIT_DATABASE_URL=sqlite:///./data/jit_access.db
    restart: unless-stopped

volumes:
  jit_data:
